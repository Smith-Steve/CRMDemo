class Contact{
    constructor(CustomerId, firstName, lastName, PhoneNumber, Email) {
        this.CustomerId = CustomerId;
        this.firstName = firstName;
        this.lastName = lastName;
        this.PhoneNumber = PhoneNumber;
        this.Email = Email;
    }
}
export default Contact